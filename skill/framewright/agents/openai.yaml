interface:
  display_name: "Framewright"
  short_description: "Storyboard, keyframe, and video prompt-file compiler"
  default_prompt: "Use $framewright to turn my scene idea into saved production-ready visual prompt files."

policy:
  allow_implicit_invocation: false
